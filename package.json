{
  "name": "data-structure-101",
  "packageManager": "yarn@3.2.0",
  "private": true,
  "engines": {
    "node": ">=16",
    "npm": "please-use-yarn"
  },
  "scripts": {
    "clean-build": "rm -rf dist",
    "build": "yarn clean-build && tsc"
  },
  "devDependencies": {
    "typescript": "^4.6.3"
  }
}
